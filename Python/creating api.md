#What_is_Python 
#apicreation 
The data will be stored in the data base, but in python we can able to add the data.

#What_is_json
- JSON is a string.
- So when you have a Python dictionary and you wanna turn
- it into JSON, you stringify it.
- You turn it into a string.
- It's important because REST APIs usually work with JSON.
- You will receive JSON data in the request and you will respond
- with JSON data in the response.
- So JSON is a long string of data.
- That's it, it's just text.
- But the contents need to follow a specific format so that the client
#data_insret_in_json
1. can then understand what is inside it.
2. Then with a comma, you separate one key and value pair from
3. another key and value pair.
4. The values can be strings, but they can also be numbers.
5. They can be integers or decimals.
6. You can have lists and you can also have sub objects.
7. There are also other types of values, like booleans true or false.
