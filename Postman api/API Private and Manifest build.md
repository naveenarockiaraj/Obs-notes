1. first have to create the branch in apit test and commit the code in there
2. clone the manifest branch in terminal and open the code change the path for which code we want
	eg...![[Pasted image 20240725124624.png]]
1. commit this code 
2. In build with parameters -- change apitest manifest name and change to which build we using build 
3. click the build 
# **private build**
- first have to add the commi branch name in manifest>>api test--- revision 
- and commite and push the manifest code.
- and take the manifest name and put in the build with parameters 
- and add the todays build number and start the build.
![[Pasted image 20240730174348.png]]![[Pasted image 20240730174624.png]]